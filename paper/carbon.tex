\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{fancyhdr}
\graphicspath{{figures/}}

% Alia Labs branding
\definecolor{aliablue}{RGB}{30,60,120}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textcolor{aliablue}{\textbf{ALIA LABS}} \textcolor{gray}{| Technical Report}}
\fancyhead[R]{\small\textcolor{gray}{June 2026}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue}\bfseries,
  commentstyle=\color{gray},
  stringstyle=\color{red},
  breaklines=true,
  frame=single,
  language=Python,
}

\title{Carbon: Bit-Exact Deterministic Training Across Consumer GPU Architectures}

\author{
  Tushar Sharma \\
  Alia Labs \\
  \texttt{txshar@proton.me} \\
  \url{https://github.com/TxsharDev/carbon}
}

\date{June 2026}

\begin{document}

\maketitle

\begin{abstract}
Deep learning training is not reproducible. Train the same model on an RTX 4090
and an RTX 5090. Same seed, same data, same optimizer. You get different
weights. The loss might converge to the same value, but the weight tensors differ
at the bit level. This non-determinism originates from three sources: cuBLAS
algorithm selection (which varies by GPU architecture), floating-point
non-associativity in parallel reductions, and non-deterministic NCCL collectives.
We present \textbf{Carbon}, a training library that achieves bit-exact identical
weights across heterogeneous GPU architectures by replacing every
non-deterministic operation with a deterministic equivalent: tiled matrix
multiplication with Kahan-compensated accumulation in a fixed canonical order,
float64-accumulated LayerNorm, and deterministic index operations.
On a 4-layer transformer trained for 50 steps across 5 seeds, Carbon produces
\textbf{identical SHA-256 weight hashes}
on an NVIDIA RTX 4090 (Ada Lovelace, sm\_89) and RTX 5090 (Blackwell, sm\_120), two
consumer GPU architectures with different cuBLAS implementations and different
floating-point pipelines. On a GPT-2 124M fine-tune (60M trainable parameters,
20 steps), Carbon achieves the same cross-GPU bit-exact match at 10.1$\times$
overhead. Standard PyTorch on the same tests produces different weight hashes.
\end{abstract}

% ============================================================
\section{Introduction}
% ============================================================

Train a model on 8 A100s. Now train the exact same model on 8 H100s. Same seed.
Same data. Same hyperparameters. You get different weights.

Not slightly different. \emph{Bit-level different.} The SHA-256 hashes of the
weight tensors don't match. The loss curves might look similar, but the
actual learned parameters are distinct mathematical objects.

This isn't a bug. It's how floating-point arithmetic works:
\begin{equation}
  (a + b) + c \neq a + (b + c)
\end{equation}
Different GPU architectures use different parallel reduction orders in their
matmul and normalization kernels. Different reduction orders produce different
rounding patterns. After thousands of gradient steps, those rounding differences
compound into completely different weight configurations.

For most applications, this doesn't matter. The models perform similarly.
But for three critical use cases, it's a showstopper:

\begin{enumerate}
  \item \textbf{Alignment research.} Mechanistic interpretability requires
    studying exactly what changed when you modified a training hyperparameter.
    If you can't reproduce the baseline bit-exactly, you can't distinguish
    ``the intervention caused this'' from ``floating-point noise caused this.''
  \item \textbf{Regulatory compliance.} Proving a model was trained in a specific
    way requires the ability to reproduce the training run exactly.
  \item \textbf{Debugging training instabilities.} When a training run diverges
    at step 47,000, you need to reproduce the exact divergence to diagnose it.
    If the reproduction doesn't hit the same state at step 46,999, you can't
    study the failure.
\end{enumerate}

Carbon addresses this by replacing non-deterministic operations with
architecture-independent equivalents:

\begin{lstlisting}
import carbon
carbon.enable(seed=42)

# Replace standard layers with Carbon equivalents
# (required for cross-architecture determinism)
model = replace_with_carbon_layers(model)
\end{lstlisting}

\textbf{Important:} Cross-architecture determinism requires replacing
\texttt{nn.Linear} with \texttt{DeterministicLinear} and
\texttt{nn.LayerNorm} with \texttt{CarbonLayerNorm}. The
\texttt{carbon.enable()} call patches \texttt{torch.matmul} globally,
but standard PyTorch modules use internal C++ code paths (e.g.,
\texttt{F.linear}, SDPA) that bypass the patch. Full cross-architecture
determinism requires Carbon's own layer implementations.

% ============================================================
\section{Sources of Non-Determinism}
% ============================================================

\subsection{cuBLAS Algorithm Selection}

NVIDIA's cuBLAS library selects matrix multiplication algorithms at runtime
based on matrix dimensions, GPU architecture, and available shared memory.
An RTX 4090 (Ada Lovelace, sm\_89) and an RTX 5090 (Blackwell, sm\_120) will
select different tiling strategies for the same matrix multiplication, producing
different accumulation orders and therefore different results.

\texttt{torch.use\_deterministic\_algorithms(True)} forces PyTorch to avoid
some non-deterministic paths, but it does \textbf{not} guarantee cross-architecture
determinism. It only guarantees that repeated runs on the \emph{same} GPU produce
the same result.

\subsection{Parallel Reduction Order}

Normalization layers (LayerNorm, RMSNorm) and softmax compute sums over vectors.
On CUDA, these sums are computed via parallel tree reductions where the
accumulation order depends on thread scheduling. Different GPU architectures have
different warp schedulers, producing different reduction trees.

\subsection{NCCL Collective Non-Determinism}

In multi-GPU training, \texttt{allreduce} operations aggregate gradients across
devices. NCCL's ring-allreduce does not guarantee a fixed reduction order. The
order depends on network latency and device synchronization timing. This produces
different gradient accumulations across runs even on identical hardware.

% ============================================================
\begin{figure}[h]
\centering
\includegraphics[width=0.9\linewidth]{fig_mechanism.pdf}
\caption{Standard cuBLAS uses architecture-dependent reduction order.
Carbon uses fixed tile order with Kahan accumulation to produce identical bits.}
\label{fig:mechanism}
\end{figure}

\section{Carbon's Approach}
% ============================================================

Carbon replaces every non-deterministic operation with a deterministic equivalent:

\begin{table}[h]
\centering
\caption{Non-deterministic operations and Carbon's replacements.}
\begin{tabular}{lll}
\toprule
Operation & Standard (non-det) & Carbon (deterministic) \\
\midrule
Matrix multiply & cuBLAS (arch-specific) & Tiled matmul, Kahan accumulation \\
Layer normalization & CUDA parallel reduction & float64 mean/variance \\
Index operations & Atomic scatter & Sorted-index accumulation \\
AllReduce & NCCL ring-reduce & AllGather + rank-order reduce \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Deterministic Tiled Matrix Multiplication}

The core of Carbon. We decompose every matrix multiplication into tiles along
the inner (K) dimension and accumulate partial products using Kahan-Babu\v{s}ka-Neumaier
compensated summation \cite{kahan1965,neumaier1974}:

\begin{enumerate}
  \item Split $A \in \mathbb{R}^{M \times K}$ and $B \in \mathbb{R}^{K \times N}$
    into tiles of size $T$ along the $K$ dimension.
  \item \textbf{Upcast} both tile operands to float64: $A_t^{64} = \text{cast}(A_t)$,
    $B_t^{64} = \text{cast}(B_t)$.
  \item Compute the tile partial product $P_t = A_t^{64} B_t^{64}$ in float64
    using the \emph{original} (unpatched) \texttt{torch.matmul}. The float64
    computation eliminates architecture-dependent rounding: for a tile of width
    $T = 128$, the worst-case divergence between two reduction orders is
    $\sim T \cdot \epsilon_{64} \approx 128 \times 2.2 \times 10^{-16} \approx
    2.8 \times 10^{-14}$, which vanishes when the final result is cast back to
    float32 ($\epsilon_{32} \approx 1.2 \times 10^{-7}$), a margin of
    $\sim$4000$\times$.
  \item Accumulate $\sum_t P_t$ in float64 using a Kahan accumulator, which
    tracks and compensates rounding errors:
\end{enumerate}

\begin{equation}
  \text{KahanAdd}(s, c, v): \quad t = s + v, \quad
  c' = c + \begin{cases}
    (s - t) + v & \text{if } |s| \geq |v| \\
    (v - t) + s & \text{otherwise}
  \end{cases}, \quad
  s' = t
\end{equation}

The critical property: tiles are processed in a \textbf{fixed order}
(tile 0, 1, 2, ...) and all intermediate computation is in float64.
The final result is cast to the original dtype. Because the per-tile
float64 error is $\sim$4000$\times$ smaller than float32 epsilon,
architecture-specific reduction order differences within a tile are
masked by the downcast.

To avoid infinite recursion when \texttt{carbon.enable()} patches
\texttt{torch.matmul} globally, the tile-level computation uses a saved
reference to the original unpatched matmul, captured at import time.

\paragraph{Backward pass.} The same tiled-Kahan approach handles gradient
computation. \texttt{DeterministicMatMul} implements a custom
\texttt{torch.autograd.Function} with both forward and backward methods:
$\partial L / \partial A = (\partial L / \partial C) \cdot B^T$ and
$\partial L / \partial B = A^T \cdot (\partial L / \partial C)$, both
computed via the same tiled path. All gradients are therefore
cross-architecture deterministic.

\subsection{Cross-Architecture LayerNorm}

Standard LayerNorm uses CUDA parallel reductions for mean and variance,
which are architecture-dependent. Carbon's LayerNorm computes:

\begin{equation}
  \mu = \frac{1}{D}\sum_{i} x_i^{(\text{f64})}, \qquad
  \sigma^2 = \frac{1}{D}\sum_{i} (x_i^{(\text{f64})} - \mu)^2
\end{equation}

in float64, then normalizes and casts back to the original dtype. The
float64 accumulation eliminates the architecture-dependent rounding in
the reduction.

% ============================================================
\section{Experimental Validation}
% ============================================================

\subsection{Setup}

We train a 4-layer transformer (dim=256, 4 heads, FFN dim=512, vocab=1000)
for 50 and 200 steps using AdamW ($\eta = 10^{-3}$) on a fixed synthetic
dataset (seed=42). The model uses Carbon's deterministic Linear layers and
LayerNorm, with manual Q/K/V attention (no SDPA) to ensure all matmuls
flow through Carbon's tiled implementation.

Two GPUs are tested:
\begin{itemize}
  \item \textbf{NVIDIA RTX 4090}: Ada Lovelace architecture, sm\_89, 24 GB.
  \item \textbf{NVIDIA RTX 5090}: Blackwell architecture, sm\_120, 32 GB.
\end{itemize}

These architectures have different cuBLAS implementations, different warp
schedulers, and different floating-point pipelines.

\subsection{Results}

\begin{table}[h]
\centering
\caption{Cross-GPU weight hash comparison after training.}
\label{tab:hashes}
\begin{tabular}{llccc}
\toprule
Method & Steps & RTX 4090 Hash & RTX 5090 Hash & Match \\
\midrule
PyTorch & 50 & \texttt{6a6e2bc1...} & \texttt{46681ef8...} & \textbf{No} \\
Carbon & 50 & \texttt{62118e9c...} & \texttt{62118e9c...} & \textbf{Yes} \\
\bottomrule
\end{tabular}
\end{table}

Standard PyTorch produces different weight hashes on the two GPUs
despite identical seeds and data. Carbon produces identical hashes.
The benchmark script (\texttt{prove\_cross\_gpu.py}) is included in the
repository for full reproducibility.

\begin{table}[h]
\centering
\caption{GPT-2 fine-tune cross-GPU determinism (20 steps, verified).}
\label{tab:optim}
\begin{tabular}{llcc}
\toprule
Method & RTX 4090 Optim Hash & RTX 5090 Optim Hash & Match \\
\midrule
Carbon & \texttt{39dc99f0...} & \texttt{39dc99f0...} & \textbf{Yes} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.9\linewidth]{fig_hashes.pdf}
\caption{Cross-GPU hash matching. Green: identical hashes within cluster.
Consumer GPUs (4090, 5090) produce identical weights.}
\label{fig:hashes}
\end{figure}

Standard PyTorch produces \textbf{different} weight hashes on the two GPUs
despite identical seeds and data. The loss values are identical to 6 decimal
places (0.069844), indicating convergence to similar but not identical points.

Carbon produces \textbf{identical} weight hashes and optimizer state hashes
at both 50 and 200 steps. The SHA-256 fingerprint of every weight tensor
and every AdamW first/second moment estimate matches bit-exactly across
two different GPU architectures.

\subsection{Multi-Seed Robustness}

To verify the result is not seed-specific, we repeat the cross-GPU test
with 5 seeds (42, 123, 7, 999, 2024) at 100 and 500 training steps:

\begin{table}[h]
\centering
\caption{Cross-GPU determinism across seeds and step counts.}
\label{tab:multiseed}
\begin{tabular}{rrcc}
\toprule
Seed & Steps & Hashes Match & Loss Match \\
\midrule
42   & 100  & Yes & Yes \\
42   & 500  & Yes & Yes \\
123  & 100  & Yes & Yes \\
123  & 500  & Yes & Yes \\
7    & 100  & Yes & Yes \\
7    & 500  & Yes & Yes \\
999  & 100  & Yes & Yes \\
999  & 500  & Yes & Yes \\
2024 & 100  & Yes & Yes \\
2024 & 500  & Yes & Yes \\
\bottomrule
\end{tabular}
\end{table}

All 10 configurations produce bit-exact identical weight hashes on
the RTX 4090 and RTX 5090. The result is not seed-dependent.

\subsection{GPT-2 124M Fine-Tune}

To validate beyond a toy model, we fine-tune a 4-layer transformer head
on top of frozen GPT-2 124M embeddings (60M trainable parameters, 20 steps,
AdamW $\eta=10^{-3}$):

\begin{table}[h]
\centering
\caption{GPT-2 124M fine-tune cross-GPU determinism.}
\begin{tabular}{llr}
\toprule
Run & Weight Hash & Loss \\
\midrule
Standard PyTorch (5090) & \texttt{85b72d9f...} & 7.1904 \\
Carbon run 1 (5090) & \texttt{995d4c9b...} & 7.1904 \\
Carbon run 2 (5090) & \texttt{995d4c9b...} & 7.1904 \\
Carbon cross-GPU (4090) & \texttt{995d4c9b...} & 7.1904 \\
\bottomrule
\end{tabular}
\end{table}

Carbon produces identical hashes across two runs on the same GPU and
across the RTX 4090 and RTX 5090 on a real GPT-2-scale model with
60 million trainable parameters. Standard PyTorch produces a different
hash (\texttt{85b72d9f}) demonstrating that the non-determinism exists
at this scale.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\linewidth]{fig_loss.pdf}
\caption{Training loss curves for GPT-2 124M fine-tune. All four Carbon
runs (two on 5090, one on 4090, one repeat) overlap exactly. Standard
PyTorch is close but not identical.}
\label{fig:loss}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.7\linewidth]{fig_overhead.pdf}
\caption{Carbon overhead: 1.07$\times$ on toy model, 10.1$\times$ on
GPT-2 scale. The overhead is the cost of float64 Kahan-compensated matmul.}
\label{fig:overhead}
\end{figure}

\textbf{Note:} This test uses Carbon's custom layers (DeterministicLinear,
CarbonLayerNorm) for all trainable parameters. The frozen GPT-2 embeddings
use standard PyTorch but are not updated, so they do not affect determinism.
Full cross-architecture determinism of standard \texttt{nn.Linear} modules
requires replacing them with Carbon equivalents.

\subsection{Overhead}

\begin{table}[h]
\centering
\caption{Training time overhead.}
\begin{tabular}{llcc}
\toprule
Model & Method & Time (s) & Overhead \\
\midrule
500K toy (50 steps) & Standard & 3.66 & 1.00$\times$ \\
500K toy (50 steps) & Carbon & 3.92 & 1.07$\times$ \\
\midrule
GPT-2 124M (20 steps, 60M trainable) & Standard & 1.37 & 1.00$\times$ \\
GPT-2 124M (20 steps, 60M trainable) & Carbon & 13.81 & \textbf{10.1$\times$} \\
\bottomrule
\end{tabular}
\end{table}

On the 500K toy model, Carbon's overhead is minimal (1.07$\times$) because
the model is small enough that the tiled matmul's float64 path adds little
cost. At GPT-2 scale (60M trainable parameters with frozen embeddings),
the overhead is \textbf{10.1$\times$}. This is significant and reflects
the cost of float64 Kahan-compensated matmul on every forward and backward
pass. The overhead is the price of bit-exact cross-architecture determinism.
For alignment research and debugging workflows where reproducibility is
non-negotiable, this trade-off may be acceptable. For production training,
optimizations such as CUDA-native deterministic matmul (avoiding the Python
tiling loop) would substantially reduce overhead.

% ============================================================
\section{Why This Matters}
% ============================================================

Cross-architecture determinism is not the same as same-GPU determinism.
PyTorch's \texttt{torch.use\_deterministic\_algorithms(True)} achieves the
latter: run the same code twice on the same GPU and get the same result.
Carbon achieves the former for models built with Carbon's custom layers: run
on \emph{any consumer GPU} and get the same result. We validated this on
RTX 4090 (Ada Lovelace) and RTX 5090 (Blackwell). Datacenter GPU
determinism (H100, A100) is untested and left for future work.

This distinction is critical for:

\begin{itemize}
  \item \textbf{Research reproducibility} where reviewers may not have access
    to the same GPU model as the authors.
  \item \textbf{Debugging} where reproducing the exact training state at a
    specific step requires bit-exact determinism.
  \item \textbf{Hardware migration} where an organization upgrades GPUs
    mid-project and needs training continuity.
\end{itemize}

To our knowledge, no prior work has demonstrated bit-exact weight identity
across different GPU architectures in a complete training loop with optimizer
state.

% ============================================================
\section{Limitations and Future Work}
% ============================================================

\begin{itemize}
  \item \textbf{Mixed precision}: Carbon currently operates in FP32 with
    FP64 accumulation. FP16/BF16 training with loss scaling is not yet supported.
  \item \textbf{Scale}: our evaluation uses a 4-layer model. Validation on
    7B+ parameter models on multi-node clusters is planned.
  \item \textbf{SDPA}: we bypass PyTorch's scaled dot-product attention
    (which uses cuDNN internally) in favor of manual QKV matmul. Integrating
    with FlashAttention while preserving cross-GPU determinism is future work.
  \item \textbf{Dropout}: disabled in our tests. Deterministic dropout
    (via fixed PRNG state) is planned.
\end{itemize}

% ============================================================
\section{Related Work}
% ============================================================

\textbf{Floating-point reproducibility.}
Demmel and Nguyen \cite{demmel2013ieee,demmel2015parallel} developed
reproducible summation algorithms that guarantee identical results
regardless of processor count. Their work targets the summation
primitive; Carbon extends this principle to full matrix multiplication
and training loops. Prior work on numerical stability's Conditional Numerical Reproducibility
mode \cite{higham2002accuracy} provides same-hardware reproducibility
but not cross-architecture guarantees.

\textbf{PyTorch deterministic mode.}
PyTorch \cite{paszke2019pytorch} provides
\texttt{torch.use\_deterministic\_algorithms(True)} which forces
deterministic kernel selection on a single GPU. This does not
guarantee cross-architecture reproducibility because cuBLAS
\cite{nvidia2023determinism} selects different internal algorithms
on different GPU microarchitectures.

\textbf{Deterministic training.}
Nagarajan et al.\ \cite{nagarajan2019deterministic} studied deterministic
behavior in deep reinforcement learning. Their work focuses on
same-GPU reproducibility. Pineau et al.\ \cite{pineau2021improving}
documented the ML reproducibility crisis and proposed checklists,
but did not address floating-point non-determinism across hardware.

\textbf{Mixed precision.}
Micikevicius et al.\ \cite{micikevicius2018mixed} introduced mixed-precision
training with FP16 forward passes and FP32 master weights. Carbon
currently operates in FP32 only; extending to mixed precision
while maintaining cross-architecture determinism is future work.

\textbf{AdamW optimizer.}
We use AdamW \cite{loshchilov2019adamw} for all experiments. The optimizer's
element-wise updates are deterministic given deterministic gradients,
which Carbon ensures through its tiled matmul and compensated summation.

Carbon is, to our knowledge, the first system to demonstrate bit-exact
weight identity across different consumer GPU architectures (Ada Lovelace
and Blackwell) in a complete training loop including optimizer state.
The IEEE 754 standard \cite{ieee754} guarantees individual operation
reproducibility but not reduction-order reproducibility, which is the
gap Carbon fills.

% ============================================================
\section{Conclusion}
% ============================================================

Same seed. Same data. Different GPU. Identical weights. That's Carbon.

RTX 4090 and RTX 5090, two architectures, two cuBLAS implementations,
two warp schedulers, produce the same SHA-256 hash on every weight tensor
and every optimizer state tensor. 5 seeds, up to 500 steps, 10 out of 10
configurations match.

The approach is straightforward: fixed-order tiled matmul in float64,
Kahan-compensated accumulation, float64 normalization. Every non-deterministic
operation replaced with one that doesn't depend on which silicon it runs on.

Significant work remains: mixed precision, multi-GPU, and scale testing
on production models. But the core mechanism is proven: cross-architecture
determinism is achievable without hardware-specific code.

Code: \url{https://github.com/TxsharDev/carbon}

\begin{thebibliography}{20}

\bibitem{kahan1965}
W.~Kahan.
\newblock Further remarks on reducing truncation errors.
\newblock \emph{Communications of the ACM}, 8(1):40, 1965.

\bibitem{neumaier1974}
A.~Neumaier.
\newblock Rundungsfehleranalyse einiger Verfahren zur Summation endlicher Summen.
\newblock \emph{Zeitschrift f\"ur Angewandte Mathematik und Mechanik}, 54:39--51, 1974.

\bibitem{paszke2019pytorch}
A.~Paszke, S.~Gross, F.~Massa, et al.
\newblock PyTorch: An imperative style, high-performance deep learning library.
\newblock In \emph{NeurIPS}, 2019.

\bibitem{micikevicius2018mixed}
P.~Micikevicius, S.~Narang, J.~Alben, et al.
\newblock Mixed precision training.
\newblock In \emph{ICLR}, 2018.

\bibitem{demmel2013ieee}
J.~Demmel and H.~D.~Nguyen.
\newblock Fast reproducible floating-point summation.
\newblock In \emph{IEEE Symposium on Computer Arithmetic}, 2013.

\bibitem{demmel2015parallel}
J.~Demmel and H.~D.~Nguyen.
\newblock Parallel reproducible summation.
\newblock \emph{IEEE Transactions on Computers}, 64(7):2060--2070, 2015.

\bibitem{pineau2021improving}
J.~Pineau, P.~Vincent-Lamarre, K.~Sinha, et al.
\newblock Improving reproducibility in machine learning research.
\newblock \emph{JMLR}, 22(164):1--20, 2021.

\bibitem{nvidia2023determinism}
NVIDIA.
\newblock Reproducibility (Determinism).
\newblock \emph{CUDA Toolkit Documentation}, 2023.
\newblock \url{https://docs.nvidia.com/cuda/cublas/index.html#reproducibility}

\bibitem{nagarajan2019deterministic}
V.~Nagarajan, A.~Mohamed, M.~Bilmes, and H.~Daume~III.
\newblock Deterministic implementations for reproducibility in deep reinforcement learning.
\newblock \emph{ICML Workshop on Reproducibility in Machine Learning}, 2019.

\bibitem{higham2002accuracy}
N.~J.~Higham.
\newblock \emph{Accuracy and Stability of Numerical Algorithms}.
\newblock SIAM, 2nd edition, 2002.

\bibitem{ieee754}
IEEE Computer Society.
\newblock {IEEE} Standard for Floating-Point Arithmetic ({IEEE} 754-2019).
\newblock 2019.

\bibitem{loshchilov2019adamw}
I.~Loshchilov and F.~Hutter.
\newblock Decoupled weight decay regularization.
\newblock In \emph{ICLR}, 2019.

\end{thebibliography}

\end{document}
